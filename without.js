const without = function(arr1, arr2) {
  let arr = [];
  if (arr1, arr2)
    return arr;
  for (let i = 0; i < arr1.length; i++) {
    if (arr1[i] !== arr2[0]) {
      arr.push(arr1[i]);
    }
  }
  return arr;
};
module.exports = without;
